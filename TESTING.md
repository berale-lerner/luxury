# TESTING.md — עקרונות ומדיניות טסטים

**לקרוא בכל פעם שכותבים טסטים.** זהו המסמך המחייב לנושא; [STANDARDS.md](STANDARDS.md) ו-[PLAN.md](PLAN.md) מפנים לכאן.

---

## הכלל הבסיסי

**כל משימה נכתבת עם טסטים.** קוד בלי טסטים אינו משימה שהושלמה.

**Skill בפרויקט:** `.claude/skills/writing-tests/` — נטען אוטומטית כשכותבים קוד או טסטים, ומכיל את מיפוי "מה שינית → אילו טסטים חובה". המסמך הזה נשאר מקור האמת; ה-Skill מפנה אליו.

⚠️ **Skill הוא הנחיה למודל, לא אכיפה.** האכיפה האמיתית היא ב-CI: הטסטים חייבים לעבור לפני merge. זה בדיוק ההבדל בין "המודל מבקש" ל"הקוד מחליט".

---

## חלוקת המאמץ

הסדר נקבע לפי "כמה באגים נתפסים ביחס לעלות הכתיבה והתחזוקה":

| שכבה | משקל | מה נבדק |
|---|---|---|
| **Backend integration** | ~60% | זרימות מלאות מול Postgres אמיתי |
| **טסטי הרשאות** | ~10% | גבולות DB — קטגוריה נפרדת, חובה |
| **Backend unit** | ~20% | לוגיקה טהורה בלבד |
| **E2E** | ~10% | 3–5 תרחישים קריטיים, לא יותר |
| **Frontend** | 0–5% | רק קומפוננטה עם לוגיקה אמיתית |

הכובד ב-backend כי שם גם הכסף וגם האבטחה. ה-frontend כאן הוא בעיקר תצוגה למשתמש אחד מהימן.

**E2E היא השכבה היקרה** — איטית, שברירית, יקרה לתחזוקה. מגבילים אותה במכוון.

---

## מול איזה DB

**Postgres אמיתי בקונטיינר. לא mock, לא DB משותף.**

- מקומית וב-CI — אותו קונטיינר Postgres נקי
- ה-schema נבנה **מקבצי ה-migration של הפרודקשן**, כולל ה-GRANTs ומדיניות ה-RLS

**למה לא mock:** כל מודל האבטחה חי ב-DB — grants, RLS, הפרדת schemas. mock יעבור בהצלחה בדיוק בתרחישים שהכי חשוב לתפוס.

**למה לא סביבת dev משותפת:**
- טסטים כותבים נתונים; ריצות מקבילות דורסות זו את זו ומייצרות כשלים שנראים אקראיים
- אי אפשר לנקות בלי להרוס את מה שמשמש לפיתוח
- **וזה מבטל את הבדיקה החשובה ביותר:** DB שחי לאורך זמן צובר שינויים ידניים. טסט הרשאות מולו עלול לעבור בזכות policy שמישהו הגדיר בקונסולה ולא קיים בקוד — ואז זה נופל בפרודקשן

**מכאן נגזר כלל:** ה-migrations הן מקור האמת היחיד ל-roles, GRANTs ו-RLS. לא הגדרות ידניות.

---

## בידוד בין טסטים

- כל טסט מתחיל ממצב ידוע
- בידוד ברמת טרנזקציה עם rollback בסוף כל טסט (או truncate) — לא הסתמכות על סדר ריצה
- טסט לא תלוי בטסט אחר ולא בשאריות שלו
- **דטרמיניזם:** בלי תלות בשעון המערכת. זמן מוזרק כתלות, לא נקרא מ-`now()` בתוך הלוגיקה. מערכת שמחשבת "תזכורת X ימים לפני" חייבת להיות נבדקת בזמן שנשלט

---

## קריאות חיצוניות

**אף פעם לא קוראים ל-API אמיתי בטסטים** — לא Mini-Hotel ולא טלגרם.

- mock ברמת ה-HTTP, לא ברמת הפונקציה — כך נבדק גם קוד הפרסינג והוולידציה של התשובה
- שומרים דוגמאות תשובה אמיתיות כ-fixtures, כולל תשובות שגיאה ותשובות חלקיות
- הסיבות: עלות, rate limits, ושבירות — טסט שתלוי בשירות חיצוני נכשל מסיבות שלא קשורות לקוד

---

## קטגוריות חובה ספציפיות לפרויקט הזה

אלה לא "עוד טסטים" — הם מגנים על הגבולות שהוגדרו ב-[CLAUDE.md](CLAUDE.md).

### 1. טסטי הרשאות
הכי זולים והכי שווים בסוויטה:
- `bot_user` מנסה לקרוא מ-`business` → **נכשל**
- אורח א' מבקש נתונים של אורח ב' → חוזר ריק (RLS)
- `bot_user` מנסה לקרוא עמודה שלא הוענקה לו → נכשל

הם גם תופסים רגרסיה אם מישהו יוסיף GRANT בעתיד.

### 2. צורת הפלט של כל tool
טסט שקובע את **קבוצת השדות המדויקת** שה-tool מחזיר. שדה שנוסף בטעות — גם אם ה-tool רשאי לקרוא אותו — מפיל טסט.

זה מה שאוכף בקוד את המבחן שב-CLAUDE.md: "אם האורח יראה בדיוק מה הפונקציה מחזירה — זה בסדר?"

רלוונטי במיוחד לתשובה מ-Mini-Hotel: שדה חדש שהם יוסיפו לא ידלוף בשקט להקשר של המודל.

### 3. ולידציה של כל פרמטר שמגיע מהמודל
לכל פרמטר — טסט לערך תקין ולערכים פסולים:
- טווח תאריכים חריג (עשרות שנים), תאריכים הפוכים, פורמט לא תקין
- מספר אנשים שלילי, אפס, עצום
- חסמים אכן נאכפים

### 4. שכבת השליחה
- הנמען נגזר מ-`conversation_id` ולא מקלט חופשי
- אין מסלול קוד שבו פלט המודל קובע נמען
- rate limit נאכף

### 5. דגל "אדם השתלט"
- מנהל שולח הודעה → הסוכן מושתק באותה שיחה
- הסוכן לא חוזר לענות בלי הפעלה מפורשת

---

## מה לא בודקים

- getters, setters, קוד של הפריימוורק
- רינדור של קומפוננטות בלי לוגיקה
- מימוש פנימי — בודקים התנהגות. טסט שנשבר מכל refactor הוא נטל, לא הגנה

---

## E2E — היכן ומתי

3–5 תרחישים בלבד, **מול staging אחרי deploy**. שם המטרה שונה: לוודא שהמערכת האמיתית עלתה ומדברת עם עצמה.

מועמדים: אורח שואל על זמינות ומקבל תשובה; המנהל נכנס לדשבורד ורואה נתון אמיתי; אישור הזמנה נשלח אחרי אישור המנהל.

🔷 מה רץ על כל PR ומה רק לפני merge. הצעה: 1–3 על כל PR, השאר לפני merge.

---

## צ'קליסט לפני שמשימה נחשבת גמורה

- [ ] יש טסטים לקוד החדש
- [ ] כל פרמטר שמגיע מהמודל — יש לו טסט ולידציה
- [ ] tool חדש — יש טסט שקובע את קבוצת השדות המוחזרת
- [ ] נגע בהרשאות / RLS / GRANT — יש טסט הרשאות
- [ ] אין קריאה ל-API חיצוני אמיתי
- [ ] הטסטים עוברים על DB שנבנה מ-migrations בלבד
- [ ] אין תלות בשעון או בסדר ריצה
