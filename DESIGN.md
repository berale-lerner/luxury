# DESIGN.md — עיצוב המערכת

מסמך חי. מתאר **מה** המערכת עושה. הכללים על **איך** מותר לבנות אותה נמצאים ב-[CLAUDE.md](CLAUDE.md).
סעיפים המסומנים 🔷 הם שאלות פתוחות שטרם הוכרעו.

---

## הקשר עסקי

עסק של דירות נופש / Airbnb. שני צרכים מרכזיים: לתת מענה ללקוחות פוטנציאליים בערוצים שבהם הם נמצאים, ולתת לבעלים תמונת מצב על העסק.

---

## אקטורים

| אקטור | מי זה | רמת אמון |
|---|---|---|
| לקוח פוטנציאלי | כל אדם מהאינטרנט, דרך וואטסאפ/אינסטגרם/טלגרם | **לא מהימן** |
| אורח קיים | מי שכבר הזמין | לא מהימן |
| הגורם האנושי / בעלים | מטפל בקריאות, צופה בדשבורדים | מהימן |

---

## אפליקציה 1 — סוכן שיחה עם לקוחות

חשוף לאינטרנט. מקבל קלט לא-מהימן. שייך ל-`apps/bot`.

### ערוצים

**מתחילים בטלגרם בלבד.** אין בו תהליך אימות עסקי ואין אישור מראש של תבניות הודעה, כך שאפשר להתחיל מיד.
וואטסאפ ואינסטגרם בהמשך — המבנה (שכבת שליחה אחת, ערוץ כפרמטר) תומך בהם מראש.

### יכולות
1. **בדיקת זמינות דירות** — מול פלטפורמת Mini-Hotel
2. **מידע על הדירות** — פרטים, תיאור, תנאים
3. **איסוף פרטי הזמנה מהלקוח** — הסוכן אוסף, לא סוגר
4. **העברה לגורם אנושי** — שליחת מייל או פתיחת קריאת שירות; משם אדם מטפל

הנקודה החשובה: הסוכן **מכין בקשה, אדם סוגר את ההזמנה**. זה תואם את כלל "פעולות רגישות" ב-CLAUDE.md ולא מצריך חריגה ממנו.

**קריאת השירות נכנסת לטבלת `tasks` אצלנו**, לא למערכת חיצונית. הטבלה כבר במבנה, היא נראית בדשבורד, ואין אינטגרציה נוספת לתחזק. מייל אפשר להוסיף כהתראה על גבי זה.

### Tools (טיוטה)

| Tool | קלט | הערות אבטחה |
|---|---|---|
| `check_availability` | תאריכים, מספר אנשים | ללא זהות משתמש בקלט. חסם על טווח תאריכים ועל מספר אנשים. whitelist על שדות התשובה מ-Mini-Hotel |
| `get_unit_info` | מזהה דירה | מידע ציבורי בלבד |
| `create_lead` / `create_request` | פרטים שנאספו | כתיבה בלבד, ליעד אחד ידוע מראש |

מגבלות לכל ה-tools: rate limit + timeout, לוג לכל קריאה, ולידציה מלאה, והחזרת שדות מותרים בלבד.

### סיכונים שזוהו בדיון
- התשובה מ-Mini-Hotel נכנסת להקשר של המודל → חובה whitelist של שדות לפני שהיא מגיעה לשם (מחירי עלות, הערות פנימיות, נתוני תפוסה — לא נכנסים)
- קריאות בלולאה לזמינות = הפצצת ה-API של Mini-Hotel על חשבוננו → rate limit לכל שיחה
- קריאות חוזרות מאפשרות מיפוי לוח התפוסה המלא → דליפת מודיעין עסקי
- הסיכון המרכזי אינו ב-tool בודד אלא ב**צירוף** היכולות של הסוכן — במיוחד שילוב של קריאת מידע עם יכולת שליחת הודעות. **הוכרע:** לסוכן אין יכולת שליחה כלל (ראה אפליקציה 2), כך שהצירוף הזה לא נוצר

---

## אפליקציה 2 — הודעות יזומות

שליחה יזומה ללקוח באחת הפלטפורמות:
- אישור הזמנה
- תזכורות לפני החופשה

### החלטה: לסוכן אין יכולת שליחה

**הקוד שולח, לא הסוכן.** אין `send_message` ברשימת ה-tools. הזרימה היא: הקוד קורא לסוכן → מקבל תשובה → הקוד שולח.

מה זה משיג: הנמען, הערוץ והתזמון נקבעים בקוד ונשלפים מה-DB. המודל לעולם לא מספק כתובת יעד, ולכן "שלח את זה למישהו אחר" אינו מסלול קיים — לא בגלל שהוא חסום, אלא בגלל שאין יכולת כזו.

מה עדיין פתוח: התוכן. אם המודל מנסח את ההודעה, אפשר להשפיע על **מה** נאמר (לא על **למי**). בנוסף אין כאן אדם שמאשר כל הודעה — הקוד שולח אוטומטית, כך שגם טעות רגילה בתוכן (מחיר/תאריך שגוי) יוצאת ללקוח.

### החלטה: אישור הזמנה — טריגר ידני, נוסח מוכן

ברגע שנסגרת הזמנה, **המנהל מפעיל ידנית** את שליחת אישור ההזמנה, מתוך נוסח מוכן (תבנית מטבלת התבניות בממשק הניהול + שדות מה-DB).

המודל אינו מעורב בניסוח כלל. כלומר עבור אישור הזמנה: הנמען מהקוד, התוכן מתבנית, הטריגר מאדם — אפס טקסט שהמודל ייצר יוצא ללקוח, ואפס מסלול שבו קלט מאורח מפעיל שליחה.

**המנהל רואה preview מלא לפני השליחה** — ההודעה עם התאריכים והמחיר כפי שהיא תצא, ואז מאשר. זה מה שהופך את הטריגר הידני גם למחסום נגד טעות בנתונים, לא רק נגד תוקף.

### החלטה: תזכורות לפני החופשה — מתוזמנות, נשלחות ידנית

המערכת מחשבת מתי כל תזכורת אמורה לצאת ומציגה למנהל רשימה של **תזכורות שהגיע זמנן**. השליחה עצמה היא לחיצה של המנהל. שום הודעה לא יוצאת מעצמה.

כלומר: התזמון הוא תזכורת למנהל, לא טריגר לשליחה. אותו מודל בדיוק כמו אישור ההזמנה — תבנית + אדם שלוחץ — רק שהמערכת זוכרת במקומו מי ומתי.

זה גם המסלול לאוטומציה בעתיד אם תרצה: מורידים את הלחיצה, כל השאר כבר בנוי.

תוכן כל ההודעות מגיע מטבלת תבניות בממשק הניהול, לפי כלל "תוכן שניתן לעריכה".

---

## אפליקציה 3 — מערכת ניהול פנימית

מאחורי auth. שייך ל-`apps/admin`. דשבורדים לבעלים:

| דשבורד | schema |
|---|---|
| מצב הדירות | `public` |
| תפוסה | `public` |
| **רווחיות** | `business` — נתונים כספיים, admin בלבד |
| רשימת לקוחות | `public` (נתונים אישיים) |
| שיחות עם לקוחות | `public` |

הרווחיות היא הסיבה שההפרדה בין ה-schemas מתחילה להיות ממשית: זה מידע שהבוט לעולם לא צריך לגעת בו.

### שיחות עם לקוחות — צפייה וכתיבה

המנהל רואה בממשק את **כל** השיחות עם הלקוחות, ויכול לכתוב להם ישירות.

**אחסון:** טבלאות השיחות יושבות ב-`public`. הבוט כותב אליהן כ-`bot_user`; הניהול קורא את כולן כ-`admin_user`. RLS מגביל את הבוט לשיחה הנוכחית בלבד, המנהל רואה הכול. אין כאן חציית גבול בין השירותים.

**כתיבה ידנית של המנהל** היא המסלול הבטוח ביותר במערכת: אדם מנסח, הנמען נבחר מה-DB, המודל לא מעורב כלל.

**דגל "אדם השתלט":** ברגע שהמנהל שולח הודעה בשיחה, הסוכן מושתק בה **אוטומטית**. חזרה להפעלה רק בלחיצה מפורשת של המנהל — לא אוטומטית אחרי זמן. עדיף בוט שקט מדי על בוט שקופץ באמצע שיחה שאדם מנהל.

**קלט לא-מהימן בממשק מורשה** — הודעות אורחים מוצגות עכשיו בתוך `apps/admin`. נגזר מכך:
- להציג כטקסט, לא כ-HTML (מניעת XSS בממשק המורשה)
- ❌ לא להזין את תוכן השיחות לשום סוכן או אנליטיקה בצד הניהול. זה קלט חיצוני, והכלל ב-CLAUDE.md אוסר על כך מפורשות. רלוונטי במיוחד לרעיון עתידי מסוג "סיכום שיחות עם AI בדשבורד"

---

## שכבת שליחת הודעות (utils משותף)

שכבה אחת ששולחת הודעות בכל הערוצים, שמשמשת את שני השירותים. פותרת את שאלת ה-credentials: אין כפילות קוד, ואין מפתח שעובר בין השירותים.

**חלוקה:** הלוגיקה, הטיפוסים והפורמט ב-`packages/shared`. ה-client מקבל credentials **כפרמטר בבנייה** — הוא לא קורא env בעצמו. כל שירות מזריק את המפתחות שלו מה-env שלו. כך `packages/shared` נשאר בלי credentials, כנדרש ב-CLAUDE.md.

**מי משתמש בזה:**
- `apps/bot` — תשובות בשיחה, בתגובה להודעה נכנסת
- `apps/admin` — אישור הזמנה, תזכורות, הודעה ידנית של המנהל

**זרימת הסוכן:** endpoint מקבל הודעה מהפלטפורמה → קורא למודל → מקבל טקסט → **הקוד** קורא ל-util ושולח. הסוכן עצמו לא מחזיק tool שליחה.

**שני כללים שמחזיקים את זה:**
1. **הנמען מגיע מההודעה הנכנסת** (הסשן/הערוץ), לעולם לא מהפלט של המודל. זה מה שהופך את "הקוד שולח" למשמעותי
2. **החתימה מקבלת `conversation_id` / `contact_id`, לא כתובת חופשית.** ה-util שולף את היעד מה-DB. פונקציה שמקבלת מספר טלפון חופשי בתהליך החשוף לאינטרנט היא בדיוק מה שהופך prompt injection לדליפה

rate limit, timeout ולוג יושבים בתוך ה-util עצמו — כל צרכן מקבל אותם אוטומטית.

---

## אינטגרציות חיצוניות

| מערכת | לשם מה | סטטוס |
|---|---|---|
| Mini-Hotel | זמינות דירות | 🔷 צריך לבדוק את ה-API והשדות שהוא מחזיר |
| וואטסאפ | ערוץ שיחה | Meta Business API (לפי CLAUDE.md) |
| אינסטגרם | ערוץ שיחה | 🔷 |
| טלגרם | ערוץ שיחה | 🔷 |
| מייל | העברה לגורם אנושי | Resend (לפי CLAUDE.md) |

---

## תשתית

**Postgres רגיל, לא Supabase.**
הסיבה: `auth.uid()` של Supabase מניח משתמש מאומת מול Supabase Auth — אבל האורחים מגיעים מטלגרם/וואטסאפ ואין להם משתמש אצלנו כלל. ה-RLS יתבסס על משתנה סשן שהקוד מגדיר לכל בקשה (`SET LOCAL`), ופוליסי שקורא אותו. זה עובד בפוסטגרס רגיל בלי תלות חיצונית.

נגזרות:
- `SERVICE_ROLE_KEY` יורד מהתמונה — הכלל לגביו ב-CLAUDE.md הופך למיותר
- הטסטים מתפשטים: אותו קונטיינר Postgres נקי מקומית וב-CI

**שירות פריסה: Railway.**
הסיבה: המודל שלו הוא כמה שירותים נפרדים מריפו אחד, כל אחד עם env vars משלו — תואם לגבול האבטחה בין `apps/bot` ל-`apps/admin`. הוא גם מספק את ה-Postgres ומאפשר סביבת staging נפרדת בקלות.

⚠️ **אזהרה תפעולית:** ב-Railway אפשר להגדיר משתני סביבה משותפים ברמת הפרויקט. זה סותר ישירות כלל ב-CLAUDE.md. **כל המשתנים ברמת השירות בלבד.**

---

## ניהול הסיסטם פרומפט — אפיון

מסך בממשק הניהול. **לכל סוכן** יש קבוצת מסמכים מסודרת; ההרכבה שלהם היא הסיסטם פרומפט שנשלח בכל קריאה.
כרגע קיים סוכן אחד (בוט האורחים), אבל המבנה מכיל סוכנים מרובים מראש.

**כל הפרומפט ניתן לעריכה** — אין קטע ליבה נעול. שום דבר בפרומפט אינו מנגנון אבטחה ממילא; גבולות האבטחה יושבים ב-tools וב-DB ולא זזים גם אם כל המסמכים יימחקו.

**הגדרות ה-tools אינן חלק מזה.** שם, תיאור וסכמת פרמטרים נשלחים למודל כמבנה נפרד ומוגדרים בקוד. המסמכים מכילים מדיניות והתנהגות בלבד.

**אחסון: ב-DB, לא בקבצים סטטיים** — כדי שהבעלים ישנה ניסוח בלי deploy.

### מודל הנתונים (טיוטה)

```
agents              id, key, name
prompt_documents    id, agent_id, title, content, order, is_active
prompt_versions     id, agent_id, version_number, snapshot, published_by, published_at
```

`snapshot` הוא צילום של **כל** המסמכים הפעילים של הסוכן ברגע הפרסום.

### גרסאות — הסט כולו, לא מסמך בודד

**החלטה:** פרסום מייצר גרסה אחת של כל הסט, וחזרה אחורה מחזירה את כולו.

הסיבה: אם כל מסמך מתוגרס בנפרד, "חזרה לגרסה קודמת" מייצרת שילוב של מסמכים שמעולם לא רץ יחד ולא נבדק. ברמת הסט אתה תמיד יודע בדיוק מה פעיל.

### טיוטה ופרסום

- העריכה נעשית על טיוטה. **הסוכן תמיד קורא את הגרסה המפורסמת האחרונה**, לא את הטיוטה
- פרסום הוא פעולה מפורשת, אחרי preview
- אי אפשר לפרסם סט בלי אף מסמך פעיל
- כל פרסום מתעד מי ומתי

### מסך הניהול — פעולות

- רשימת המסמכים של הסוכן, לפי הסדר
- יצירת מסמך חדש
- עריכת מסמך
- שינוי סדר
- הפעלה/כיבוי של מסמך (בלי למחוק אותו)
- מחיקת מסמך
- **preview** — הפרומפט המורכב במלואו, בדיוק כפי שהסוכן יקבל אותו
- פרסום
- היסטוריית גרסאות + חזרה לגרסה קודמת

### מכניקת ההרכבה בזמן ריצה

- שרשור המסמכים הפעילים לפי `order`, עם מפריד ברור בין מסמכים
- מסמך לא פעיל אינו נכנס
- הסט המפורסם נשמר ב-cache; ה-cache מתאפס בפרסום
- **מזהה הגרסה נרשם בלוג בכל קריאה לסוכן** — כדי שאפשר יהיה לדעת איזו גרסה ייצרה תשובה מסוימת

שיחה שכבר רצה עוברת לגרסה החדשה מרגע הפרסום. לא נועלים שיחה לגרסה שהתחילה בה — התיעוד בלוג נותן את יכולת הדיבוג בלי הסיבוך.

### מה אסור שייכנס למסמכים

❌ **הוראות כמנגנון אבטחה.** "אל תגלה מחירי עלות" אינו הגנה — ההגנה היא שה-tool לא מחזיר את השדה
❌ **סודות** מכל סוג

**המסך מציג את זה למנהל.** הערה קבועה במסך העריכה: הוראות בפרומפט אינן הגנה — אם אסור שהלקוח יראה משהו, הוא לא אמור לחזור מה-tool מלכתחילה. זה המקום הסביר ביותר שבו מישהו יכתוב "אל תספר ללקוח על X" ויחשוב שהגן על משהו.

### מסמך "מידע על הדירות" — הפרדה נדרשת

נתוני הדירות כבר קיימים ב-DB ומוחזרים דרך `get_unit_info`. שכפול שלהם לפרומפט יוצר שני מקורות אמת — עדכון מחיר בטבלה לא יעדכן את הפרומפט, והמודל נוטה להעדיף את מה שכתוב לו בפרומפט.

**ההפרדה:** המסמך קובע **איך מדברים** על הדירות (סגנון, מה להדגיש, מה לא להבטיח). ה-tool מספק את **העובדות**.

### הערה על עלות

כל מסמך שגדל מתורגם ליותר טוקנים בכל הודעה. יש תמריץ אמיתי לקצר.

---

## גישה לממשק הניהול

**התחברות עם גוגל, דרך Auth.js** (ספרייה, לא ספק מנוהל — הכלל ב-CLAUDE.md עודכן בהתאם: אסור להרכיב OAuth ביד, ספרייה בוגרת מותרת).

**הגישה עצמה נשמרת על ידי allowlist, לא על ידי ההתחברות.** כל אדם עם חשבון גוגל יכול להשלים את זרימת ההתחברות בהצלחה — לכן רשימת המיילים המורשים נבדקת בצד השרת מול הפרופיל שגוגל מחזירה. הרשימה יושבת בטבלה ב-DB עם מסך בממשק הניהול, כדי להוסיף מנהל בלי deploy.

ה-credentials של גוגל קיימים ב-`apps/admin` בלבד.

---

## שאלות פתוחות

1. **מה מחזיר ה-API של Mini-Hotel** — קובע את ה-whitelist של `check_availability`. חוסם מימוש

---

## סטטוס החלטות טסטים

נדונו ב-[PLAN.md](PLAN.md), טרם נכתבו כהחלטה סופית: דגש על backend integration מול Postgres ארעי, טסטי הרשאות כקטגוריה נפרדת, E2E דק מול staging.
