# STANDARDS.md — סטנדרטים לקוד

תקף לכל הפרויקט. משלים את [CLAUDE.md](CLAUDE.md) (גבולות אבטחה) ואת [DESIGN.md](DESIGN.md) (מה המערכת עושה).
סעיפים המסומנים 🔷 טרם הוכרעו.

---

## TypeScript

- `strict: true`. בלי יוצאים מן הכלל
- **בלי `any`.** בגבולות מערכת משתמשים ב-`unknown` ומצמצמים משם
- טיפוסים משותפים יושבים ב-`packages/shared`

**הנקודה החשובה:** טיפוסים של TypeScript נעלמים בזמן ריצה. הם לא ולידציה.
בכל גבול שבו נכנס מידע מבחוץ — פלט של המודל, תשובה מ-Mini-Hotel, webhook מטלגרם, גוף בקשה — נדרשת **ולידציה בזמן ריצה** (Zod או שקול לו), ולא רק הצהרת טיפוס. זה המימוש הישיר של "אל תניח שקלט מהמודל תקין" מ-CLAUDE.md.

```ts
// לא מספיק
const body = req.body as CreateRequestInput;

// נדרש
const body = createRequestSchema.parse(req.body);
```

---

## הגנה על endpoints

**ברירת המחדל היא סגור.** ההגנה נאכפת ב-middleware ברמת ה-router, לא בכל handler בנפרד — handler שנשכח נכשל **סגור** ולא פתוח. endpoint ציבורי הוא חריג שנרשם במפורש.

### `apps/admin`
כל endpoint דורש סשן מחובר **וגם** מעבר ב-allowlist (ראה "Admin access" ב-CLAUDE.md). התחברות לבדה אינה מספיקה.

### `apps/bot`
כאן "רק למי שמחובר" לא חל — ה-webhook של טלגרם הוא לפי הגדרתו נקודה ציבורית שאין מאחוריה משתמש מחובר. ההגנה עליו שונה:

- אימות ש**הבקשה באמת הגיעה מטלגרם** (secret token בכותרת ה-webhook, נבדק בכל בקשה)
- rate limiting לפי שיחה ולפי IP
- ולידציה מלאה על גוף הבקשה לפני כל שימוש

זהות האורח נגזרת מתוך ה-payload המאומת של טלגרם — לעולם לא מפרמטר שהמודל סיפק.

---

## לוגים

- לוגים **מובנים** (JSON), לא שרשור מחרוזות
- **correlation id** לכל בקשה, שנשמר לאורך כל הזרימה — כך אפשר לשחזר שיחה שלמה מקצה לקצה
- כל קריאת tool נרשמת: שם, פרמטרים אחרי ולידציה, משך, הצלחה/כישלון
- כל קריאה חיצונית (Mini-Hotel, טלגרם) נרשמת עם משך וסטטוס
- רמות: `error` / `warn` / `info` / `debug`

**מה אסור שייכנס ללוג:**
- credentials, טוקנים, מפתחות — בשום צורה
- ה-prompt המלא או ההקשר שנשלח למודל
- פרטים אישיים מלאים של אורחים (טלפון, מייל) — מזהים בלבד

- **תוכן הודעות — לא נכנס ללוגים.** השיחות נשמרות ב-DB לצורך הממשק; שכפול שלהן ללוגים מייצר עותק שני של מידע אישי במקום עם מדיניות שמירה אחרת. בלוג נרשמים מזהי שיחה והודעה בלבד

---

## קוד נקי

- פונקציה עושה דבר אחד. אם צריך "ו" בשם — כנראה שתיים
- שמות מפורשים על פני קיצורים
- **בלי בליעת שגיאות.** `catch` שלא מטפל ולא מדווח אסור. נכשלים ברעש, לא בשקט
- בלי קוד מת ובלי קוד בהערות. גיט זוכר
- פורמט ולינטינג אוטומטיים (Prettier + ESLint) — לא נושא לדיון ב-review
- ערכים שהבעלים אמור לשנות (מחירים, טקסטים, מדיניות) לא בקוד — בטבלה בממשק הניהול, לפי CLAUDE.md

---

## הפרדה בין tools ל-use cases

**זו ההפרדה החשובה ביותר בקוד הזה.**

| שכבה | תפקיד | יודעת על המודל? |
|---|---|---|
| **use case** | הלוגיקה העסקית האמיתית | לא |
| **tool** | מתאם דק שחושף use case לסוכן | כן |

**ה-use case** לא יודע שקיים סוכן. אין בו מחרוזות פרומפט, אין בו הנחות על מי קורא לו. הוא נבדק בטסטים כלוגיקה רגילה.

**ה-tool** עושה בדיוק שלושה דברים:
1. ולידציה של הפרמטרים שהגיעו מהמודל
2. קריאה ל-use case
3. **צמצום הפלט** לשדות שמותר לאורח לראות

בלי לוגיקה עסקית ב-tool. בלי צמצום פלט ב-use case.

**למה זה לא סגנון אלא אבטחה:** אותו use case משמש גם את הניהול וגם את הבוט, וכל צד רשאי לראות דברים אחרים. אם ה-tool *הוא* ה-use case, אי אפשר להשתמש בו מחדש בלי לדלוף. הצמצום חייב לשבת בשכבה שיודעת מי הקורא — כלומר ב-tool.

זה גם מה שהופך את המבחן שב-CLAUDE.md ("אם האורח יראה בדיוק מה הפונקציה מחזירה — זה בסדר?") לבדיקה על קובץ אחד קטן, במקום על כל הלוגיקה.

אותו עיקרון חל על **שכבת שליחת ההודעות**: הקוד קורא ל-util, הסוכן לא מחזיק tool שליחה כלל (ראה DESIGN.md).

---

## טסטים

כל משימה נכתבת עם טסטים. **המסמך המחייב: [TESTING.md](TESTING.md)** — לקרוא בכל פעם שכותבים טסטים.
